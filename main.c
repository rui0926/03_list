#include <stdio.h>
#include <stdlib.h>

 void efface()
int main(int argc, char *argv[]) 
{
    char str[5];
    array[100];
    
    while(fgets(str, sizeof(str), stdin))
    {
       inputnum = 0;
       int input = atoi(str);
        
        void efface()
        {
            retturn;
        }
        if (input == -1)
        {
          printf("%s\n", array[-insert-1]);
            efface
        }
         else if(input == 0)
         {
             for(i = 0, 0 <= array[insert], i++)
             {
                 if(i == 0)
                 printf("%s\n",array[0]);
                 else
                 sprintf(insert, "%s",array[i]);
                 printf("\n");
             }
          }
             printf("%s", str);
         }
        else
        {
            array[inputnum++] = insert;
        }
    }

    return 0;
}
