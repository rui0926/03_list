#include <stdio.h>

int main(int argc, char *argv[]) 
{
    char str[5];
    array[100];
    
    while(fgets(str, sizeof(str), stdin))
    {
       inputnum = 0;
        
        void efface()
        if (str == -1)
        {
          printf("%s\n", array[-insert-1]);
            efface
        }
         else if(str == 0)
         {
             for(i = 0, )
             {
                 if(i == 0)
                 printf("%s")
             }
             printf("%s", str);
         }
        else
        {
            array[inputnum++] = insert;
        }
        
       
    }

    return 0;
}
